suites:
    main:
        namespace: LaraTicket
        psr4_prefix: LaraTicket
        src_path: app

extensions:
    - PhpSpec\Laravel\Extension\LaravelExtension

laravel_extension:
    testing_environment: 'testing'

formatter.name: pretty